class Solution:
    def isSameAfterReversals(self, num: int) -> bool:
        x=str(num)
        x=int(x[::-1])
        x=int(str(x)[::-1])
        if num==x:
            return True
        else:
            return False
